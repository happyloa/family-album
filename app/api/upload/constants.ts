import {
  MAX_IMAGE_SIZE_BYTES,
  MAX_IMAGE_SIZE_MB,
  MAX_VIDEO_SIZE_BYTES,
  MAX_VIDEO_SIZE_MB,
  getSizeLimitByMime
} from '@/lib/upload/constants';

export { MAX_IMAGE_SIZE_BYTES, MAX_IMAGE_SIZE_MB, MAX_VIDEO_SIZE_BYTES, MAX_VIDEO_SIZE_MB, getSizeLimitByMime };

export const MAX_TOTAL_SIZE_MB = 400;
export const MAX_FILE_COUNT = 20;
